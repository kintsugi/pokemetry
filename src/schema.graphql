type Pokemon {
  name: String!
  height: Int!
  weight: Int!
}

type Stats {
  mean: Float!
  median: Float!
  mode: Float!
}

type Query {
  size(name: String!): Pokemon
  sizes(names: [String!]!): [Pokemon]
  heights(names: [String!]!): Stats
  weights(names: [String!]!): Stats
}
